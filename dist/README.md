# dist/

Published DIGITAL GARDEN Matchbox packages live here.

- `manifest.json` — release metadata (name, type, minimum Flame version, filenames)
- `shaders/*.mx` — encrypted Matchbox (and later Lightbox) packages only

No GLSL, XML, `.p`, thumbnails, or presets belong in this tree.
