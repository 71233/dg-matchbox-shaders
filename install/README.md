# install/

Public (GitHub) installer for DIGITAL GARDEN Matchbox packages.

```bash
./install/install.sh           # install .mx from ../dist/shaders
./install/install.sh --dry-run # show actions only
```

Internal fleet SSH deployment lives under `private/fleet/` (local only) and is redesigned separately.
